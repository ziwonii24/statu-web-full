import React, { FunctionComponent } from 'react'

interface Interface {
    mainCategoryId: number
}

const SubCategoryGroup: FunctionComponent<Interface> = (props: Interface) => {

    const { mainCategoryId } = props

    return (
        <div>

        </div>
    )
}

export default SubCategoryGroup